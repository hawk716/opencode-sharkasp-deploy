{
  "name": "opencode-deploy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-opencode": "npm install -g opencode-ai",
    "install-opencode-curl": "curl -fsSL https://opencode.ai/install | bash",
    "start": "opencode web --hostname 0.0.0.0 --port 3000 --password o",
    "dev": "npm run start"
  },
  "dependencies": {
    "opencode-ai": "^1.17.7"
  }
}
